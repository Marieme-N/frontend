import {createMuiTheme} from '@material-ui/core';

export const templateTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#ff9900'
        },
        secondary: {
            main: '#9800ff',
            contrastText: '#f5f5f5'
        }
    }
});